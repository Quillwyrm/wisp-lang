reader:
  nil / bool / int / float / string
  symbols/names
  comments
  source lists
  vector literals
  no quote
  no string escapes

compiler/runtime:
  file forms
  value def
  lexical do
  local name reads
  local set
  indexed vector set
  vector literals
  vector calls
  ordinary native calls
  core + - * /
  core push / pop
  print
  run_string
  run_file
  eval CLI mode
  file extension CLI sugar
